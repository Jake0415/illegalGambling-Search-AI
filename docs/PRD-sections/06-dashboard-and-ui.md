# 3.6 대시보드 및 UI 기능 요구사항

> 작성일: 2026-03-14
> 담당: frontend-ui-specialist
> 참조: service-concept.md, decisions.md, 01-overview-and-stories.md (US-A3, US-A5, US-A6, US-A7, US-B1, US-B3, US-C1, US-C2, US-C3, US-D1, US-D2, US-D3), 02-evidence-collection-engine.md (FR-EC-025, FR-EC-032), 04-evidence-integrity.md (FR-EV-003, FR-EV-009, FR-EV-015, FR-EV-016), 05-detection-engine.md (FR-DE-013, FR-DE-030)

---

## 개요

본 섹션은 불법 도박 사이트 자동 검색/채증 시스템의 프론트엔드 대시보드 및 UI 기능 요구사항을 정의한다. 대시보드는 사행산업통합감독위원회, 경찰청 사이버수사국, 한국마사회 불법경마 감시팀, 국민체육진흥공단 등 4개 기관의 운영자, 수사관, 관리자/감독관, 법무/증거 담당자가 사용하는 통합 웹 인터페이스이다.

시스템 운영 흐름에 따라 대시보드는 다음 핵심 기능을 제공한다:
1. **메인 대시보드** — 핵심 KPI, 실시간 활동 피드, 시스템 상태 모니터링
2. **사이트 관리** — 탐지된 불법 도박 사이트의 조회, 등록, 상태 관리
3. **채증 모니터링** — 1~3단계 채증 파이프라인 실시간 진행 상황
4. **증거 관리** — 증거 패키지 조회, 무결성 검증, 보고서 생성
5. **수동 개입** — CAPTCHA/OTP 수동 풀이 큐, 원격 브라우저 뷰어
6. **AI 분류 검토** — 저신뢰 AI 분류 결과의 수동 검토
7. **통계 및 리포트** — 기간별/유형별 통계, 네트워크 시각화, 정기 보고서
8. **시스템 설정** — 사용자/역할 관리, 외부 서비스 설정, 감사 로그

### 설계 프로세스: 화면 정의서 기반 구현

본 프로젝트는 UI 코드 구현 전에 **텍스트 기반 화면 정의서(UI Spec)**를 먼저 작성하는 2단계 프로세스를 따른다. 화면 정의서는 기획자와 개발자 간 UI/UX 설계를 사전에 합의하고, 구현 시 참조하는 설계 문서 역할을 한다.

**화면 정의서에 포함할 항목:**
1. **페이지 목적** -- 사용자 니즈와 해결하는 문제
2. **레이아웃 구조** -- ASCII 와이어프레임으로 영역 배치
3. **컴포넌트 목록** -- shadcn/ui 기본 컴포넌트 + 커스텀 컴포넌트
4. **데이터 바인딩** -- mock 서비스/API 엔드포인트 매핑
5. **사용자 인터랙션** -- 클릭, 필터, 정렬, 모달, 네비게이션 등
6. **PRD 요구사항 매핑** -- 해당 페이지가 충족하는 FR-UI-xxx 목록

**페이지별 화면 정의서 파일:**

| 화면 정의서 | 파일 경로 | 대상 페이지 | PRD 요구사항 |
|------------|----------|------------|-------------|
| 랜딩 페이지 | `docs/ui-specs/landing.md` | `/` (비인증) | FR-UI-036~038 |
| 메인 대시보드 | `docs/ui-specs/dashboard.md` | `/` (인증) | FR-UI-001~004 |
| 사이트 관리 | `docs/ui-specs/sites.md` | `/sites`, `/sites/[id]`, `/sites/new`, `/sites/import` | FR-UI-005~010 |
| 채증 모니터링 | `docs/ui-specs/investigations.md` | `/investigations`, `/investigations/[id]`, `/investigations/gallery`, `/investigations/[id]/results` | FR-UI-011~014 |
| 증거 관리 | `docs/ui-specs/evidence.md` | `/evidence`, `/evidence/[id]/verify`, `/evidence/[id]/report`, `/evidence/[id]/audit` | FR-UI-015~018 |
| CAPTCHA/OTP 수동 개입 | `docs/ui-specs/captcha-queue.md` | `/investigations/captcha-queue` | FR-UI-019~021 |
| AI 분류 검토 | `docs/ui-specs/review.md` | `/review`, `/review/[id]` | FR-UI-022~023 |
| 통계 및 분석 | `docs/ui-specs/analytics.md` | `/analytics` | FR-UI-024~026 |
| 보고서 | `docs/ui-specs/reports.md` | `/reports` | FR-UI-027~028 |
| 시스템 설정 | `docs/ui-specs/settings.md` | `/settings`, `/settings/users`, `/settings/keywords`, `/settings/audit-log` | FR-UI-029~032 |
| SMS 비용 대시보드 | `docs/ui-specs/sms-cost.md` | (설정 내 위젯) | FR-SMS-018~021 |
| 로그인/회원가입 | `docs/ui-specs/auth.md` | `/login`, `/signup` | FR-API-001 |
| 초기 설정 위저드 | `docs/ui-specs/setup-wizard.md` | `/setup` | 슈퍼어드민 계정 구조 |
| 반응형/다크모드 | `docs/ui-specs/responsive-darkmode.md` | (공통) | FR-UI-033~035 |
| 통합 검증 체크리스트 | `docs/ui-specs/integration-checklist.md` | (전체) | 전체 UI 요구사항 |

### 핵심 기술 스택

| 영역 | 채택 기술 | 근거 |
|------|----------|------|
| 프레임워크 | Next.js 15.5.3 (App Router + Turbopack) | 서버 컴포넌트, 스트리밍 SSR, 레이아웃 기반 라우팅 (decisions.md 확정) |
| UI 런타임 | React 19.1.0 + TypeScript 5 | 최신 React 기능 (use, Suspense 개선 등) |
| UI 컴포넌트 | shadcn/ui (new-york style) + Radix UI | 접근성 기본 제공, 커스터마이징 용이, TailwindCSS v4 통합 |
| 스타일링 | TailwindCSS v4 | 유틸리티 기반 CSS, 다크 모드 내장 지원 |
| 데이터 테이블 | TanStack Table v8 | 서버사이드 정렬/필터/페이지네이션, 가상화 지원 |
| 차트/시각화 | Recharts | React 네이티브 차트 라이브러리, 반응형 |
| 네트워크 그래프 | D3.js / Cytoscape.js | 도메인 호핑 네트워크 시각화 |
| 폼 관리 | React Hook Form + Zod | 서버 액션 통합, 스키마 기반 유효성 검증 |
| 인증 | NextAuth.js v5 | 역할 기반 접근 제어(RBAC), 세션 관리 |
| 상태 관리 | Zustand + TanStack Query v5 | 클라이언트 상태(Zustand) + 서버 상태(TanStack Query) 분리 |
| 실시간 통신 | Server-Sent Events (SSE) / WebSocket | 채증 진행 상황, CAPTCHA 큐 실시간 업데이트 |
| 아이콘 | Lucide Icons | 경량, Tree-shaking 지원, shadcn/ui 기본 아이콘 |

### 라우트 구조 (예상)

```
/                              → 랜딩 페이지 (비인증) 또는 메인 대시보드 (인증) (FR-UI-036~038, FR-UI-001~004)
/sites                         → 사이트 목록 (FR-UI-005)
/sites/[id]                    → 사이트 상세 (FR-UI-006)
/sites/new                     → 수동 URL 등록 (FR-UI-007)
/sites/import                  → 벌크 URL 임포트 (FR-UI-009)
/investigations                → 채증 작업 큐 (FR-UI-011)
/investigations/[id]           → 채증 세션 상세 (FR-UI-012)
/investigations/gallery        → 채증 결과 갤러리 (FR-UI-013)
/investigations/[id]/results   → 채증 결과 상세 (FR-UI-014)
/investigations/captcha-queue  → CAPTCHA/OTP 수동 개입 (FR-UI-019)
/evidence                      → 증거 패키지 목록 (FR-UI-015)
/evidence/[id]/verify          → 증거 무결성 검증 (FR-UI-016)
/evidence/[id]/report          → 증거 보고서 (FR-UI-017)
/evidence/[id]/audit           → 증거 감사 로그 (FR-UI-018)
/review                        → AI 분류 검토 큐 (FR-UI-022)
/review/[id]                   → AI 분류 상세 검토 (FR-UI-023)
/analytics                     → 통계 대시보드 (FR-UI-024~026)
/reports                       → 정기 보고서 목록 (FR-UI-027~028)
/settings                      → 시스템 설정 (FR-UI-030)
/settings/users                → 사용자/역할 관리 (FR-UI-029)
/settings/keywords             → 키워드 관리 (FR-UI-031)
/settings/audit-log            → 감사 로그 (FR-UI-032)
```

---

## 0. 랜딩 페이지 (Phase 2)

> 서비스 소개 및 로그인 유도를 위한 공개 랜딩 페이지. 인증 없이 접근 가능하며, 시스템의 핵심 가치와 기능을 소개하여 대상 기관 담당자의 관심을 유도한다.

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 | 기술 구현 참고 |
|----|------|------|---------|-------|----------|---------------|
| **FR-UI-036** | 랜딩 페이지 — 히어로 섹션 및 CTA | 루트 경로(`/`)에 비인증 사용자를 위한 공개 랜딩 페이지를 제공한다. 히어로 섹션 구성: (1) 서비스명(illegalGambling-Search-AI)과 핵심 가치 제안 헤드라인("불법 도박 사이트를 자동으로 탐지하고, 법적 증거를 확보합니다"), (2) 서브 헤드라인(시스템 핵심 차별점 요약), (3) CTA 버튼 — "로그인" 버튼(`/login`으로 이동) 및 "데모 요청" 버튼(문의 이메일 또는 외부 폼 링크), (4) 히어로 배경에 시스템 대시보드 목업 이미지 또는 그라디언트 배경. 인증된 사용자가 `/`에 접근하면 대시보드로 자동 리디렉트한다. 페이지 전체에 반응형 레이아웃과 다크 모드를 지원한다. | P1 | Phase 2 | 1) 비인증 사용자가 `/`에 접근하면 랜딩 페이지가 표시된다. 2) 인증된 사용자가 `/`에 접근하면 대시보드(`/dashboard` 또는 메인 대시보드)로 리디렉트된다. 3) "로그인" CTA 클릭 시 `/login` 페이지로 이동한다. 4) "데모 요청" CTA 클릭 시 문의 링크 또는 이메일 클라이언트가 열린다. 5) 히어로 섹션이 모바일/태블릿/데스크톱에서 정상 표시된다. 6) 다크 모드에서 배경, 텍스트, CTA 버튼 색상이 적절히 전환된다. | Next.js 15 App Router (루트 `page.tsx`에서 인증 상태 분기), shadcn/ui `Button`, TailwindCSS v4 (그라디언트, 반응형), `next-themes` (다크 모드) |
| **FR-UI-037** | 랜딩 페이지 — 주요 기능 소개 및 시스템 구성도 | 랜딩 페이지에 시스템의 핵심 기능을 카드 형태로 소개하고, 전체 파이프라인을 시각적 다이어그램으로 표시한다. 주요 기능 카드(4~6개): (1) 자동 탐지 — Google Custom Search + Scrapy 기반 불법 사이트 자동 검색, (2) 3단계 채증 — 메인화면/배팅시작/실제 배팅 자동 스크린샷 수집, (3) AI 분류 — Claude Haiku 4.5 기반 불법 도박 사이트 자동 분류(F1 94%+), (4) 증거 무결성 — SHA-256 해시 + OpenTimestamps + RFC 3161 이중 타임스탬프, (5) SMS 자동 인증 — Non-VoIP 가상번호 기반 회원가입 자동화, (6) 법원 제출용 보고서 — 증거 패키지 자동 생성. 시스템 구성도 섹션에는 전체 파이프라인(탐지 → 접속 → 채증 → 증거 패키지 생성)을 SVG 또는 CSS 기반 다이어그램으로 시각화한다. 각 기능 카드에 아이콘(Lucide Icons)과 간결한 설명을 포함한다. | P1 | Phase 2 | 1) 4~6개의 기능 소개 카드가 반응형 그리드(데스크톱 3열, 태블릿 2열, 모바일 1열)로 표시된다. 2) 각 카드에 아이콘, 기능명, 설명 텍스트가 포함된다. 3) 시스템 구성도가 전체 파이프라인을 시각적으로 표현한다. 4) 다크 모드에서 카드 및 다이어그램 색상이 적절히 전환된다. 5) 기능 카드에 hover 애니메이션(스케일 또는 그림자 변화)이 적용된다. | shadcn/ui `Card`, Lucide Icons, TailwindCSS v4 (그리드, 애니메이션), SVG 또는 CSS 기반 다이어그램 (React 컴포넌트), `next/image` (목업 이미지) |
| **FR-UI-038** | 랜딩 페이지 — 대상 기관 및 푸터 | 랜딩 페이지에 서비스 대상 기관을 소개하고, 연락처 및 법적 고지를 포함하는 푸터를 제공한다. 대상 기관 섹션: (1) 사행산업통합감독위원회, (2) 경찰청 사이버수사국, (3) 한국마사회 불법경마 감시팀, (4) 국민체육진흥공단. 각 기관에 로고(또는 플레이스홀더 아이콘)와 활용 시나리오 한 줄 설명을 표시한다. 푸터 구성: (1) 연락처 정보(이메일, 전화번호 또는 담당 부서), (2) 법적 고지("본 시스템은 인가된 수사 기관 및 감독 기관만 사용할 수 있습니다"), (3) 저작권 표시, (4) 개인정보처리방침/이용약관 링크(선택). 푸터는 반응형으로 모바일에서 수직 정렬된다. | P2 | Phase 2 | 1) 4개 대상 기관이 아이콘/로고와 함께 표시된다. 2) 각 기관에 활용 시나리오 설명이 표시된다. 3) 푸터에 연락처, 법적 고지, 저작권 표시가 포함된다. 4) 푸터가 모바일에서 수직 정렬로 정상 표시된다. 5) 다크 모드에서 푸터 배경 및 텍스트 색상이 적절히 전환된다. | shadcn/ui `Card`, Lucide Icons (기관 아이콘), TailwindCSS v4 (반응형 그리드, 푸터 레이아웃) |

---

## A. 메인 대시보드 (Phase 1)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 | 기술 구현 참고 |
|----|------|------|---------|-------|----------|---------------|
| **FR-UI-001** | 대시보드 홈 — 핵심 KPI 카드 | 대시보드 메인 페이지(`/`)에 핵심 성과 지표(KPI)를 카드 형태로 표시한다. 표시 항목: (1) 총 탐지 사이트 수 및 금일 신규 탐지 수, (2) 채증 성공률 (3단계 완료 기준, 목표 70% 이상), (3) 현재 진행 중인 채증 작업 수, (4) 증거 무결성 검증 통과율 (목표 100%). 각 KPI 카드에는 전일/전주 대비 증감률을 화살표와 색상(증가: 초록, 감소: 빨강)으로 표시하고, 클릭 시 해당 상세 페이지로 이동한다. 데이터는 서버 컴포넌트에서 직접 DB 조회하여 초기 로드 성능을 최적화한다. | P0 | Phase 1 | 1) 4개 KPI 카드(탐지 사이트 수, 채증 성공률, 진행 중 작업, 무결성 검증률)가 대시보드 상단에 표시된다. 2) 각 카드에 전일 대비 증감률이 화살표 아이콘과 함께 표시된다. 3) 카드 클릭 시 관련 상세 페이지(`/sites`, `/investigations`, `/evidence`)로 이동한다. 4) 페이지 초기 로드 시간이 2초 이내이다 (서버 컴포넌트 기준). 5) 스켈레톤 UI가 데이터 로딩 중 표시된다. | Next.js 15 Server Components, shadcn/ui `Card` 컴포넌트, Lucide Icons (TrendingUp/TrendingDown), TanStack Query (클라이언트 리프레시) |
| **FR-UI-002** | 최근 활동 피드 — 탐지/채증 타임라인 | 대시보드 메인 페이지에 최근 시스템 활동을 타임라인(피드) 형태로 표시한다. 활동 유형: (1) 신규 사이트 탐지, (2) 채증 시작/완료/실패, (3) AI 분류 완료, (4) 도메인 상태 변경, (5) CAPTCHA/OTP 수동 개입 요청, (6) 증거 패키지 생성. 각 활동 항목에 아이콘, 타임스탬프(상대 시간: "3분 전"), 사이트명/URL, 상태 배지를 표시하며, 최근 50건을 기본 로드하고 무한 스크롤로 추가 로드한다. Server-Sent Events(SSE)로 신규 활동을 실시간 푸시한다. | P0 | Phase 1 | 1) 최근 활동이 시간 역순으로 타임라인 형태로 표시된다. 2) 6가지 활동 유형별로 구분 아이콘과 색상이 적용된다. 3) 신규 활동이 SSE를 통해 실시간으로 피드에 추가된다 (폴링 대비 지연 3초 이내). 4) 무한 스크롤로 과거 활동을 추가 로드할 수 있다. 5) 활동 유형별 필터링이 가능하다. | Next.js Route Handler (SSE 엔드포인트), shadcn/ui `ScrollArea`, `Badge`, React 19 `use()` 훅, TanStack Query `useInfiniteQuery` |
| **FR-UI-003** | 채증 진행 상황 실시간 모니터링 위젯 | 대시보드 메인 페이지에 현재 진행 중인 채증 작업의 실시간 진행 상황을 모니터링하는 위젯을 제공한다. 각 채증 작업에 대해: (1) 대상 사이트 URL, (2) 현재 단계(1단계/2단계/3단계) 및 세부 스텝(접속→팝업처리→스크린샷→HTML저장 등), (3) 진행률 프로그레스바, (4) 경과 시간, (5) 상태 표시(정상 진행/대기/오류)를 표시한다. 최대 동시 표시 작업 수는 10개이며, 초과 시 "외 N건 진행 중" 요약을 표시한다. WebSocket 또는 SSE로 0.5~1초 간격 업데이트한다. | P1 | Phase 1 | 1) 진행 중인 채증 작업이 개별 카드 형태로 표시된다. 2) 각 작업의 현재 단계와 세부 스텝이 프로그레스바로 시각화된다. 3) 상태 변경이 SSE를 통해 1초 이내에 UI에 반영된다. 4) 오류 발생 시 해당 작업 카드에 빨간색 경고 표시가 즉시 나타난다. 5) 작업 카드 클릭 시 채증 세션 상세 페이지(`/investigations/[id]`)로 이동한다. | SSE 또는 WebSocket, shadcn/ui `Progress`, `Card`, BullMQ 이벤트 연동, Zustand (실시간 상태 관리) |
| **FR-UI-004** | 시스템 상태 모니터링 위젯 | 대시보드에 시스템 전반의 운영 상태를 모니터링하는 위젯을 제공한다. 표시 항목: (1) 활성 브라우저 인스턴스 수 / 최대 허용 수, (2) BullMQ 큐 상태 (대기/진행/완료/실패 건수), (3) 외부 서비스 상태 — SMS 서비스(PVAPins/GrizzlySMS) 연결 상태 및 잔여 크레딧, CAPTCHA 솔버(CapSolver/2Captcha) 연결 상태 및 잔액, 프록시 서비스 가용 IP 수 및 성공률, Claude API 할당량 사용률, (4) Redis 연결 상태, (5) PostgreSQL 연결 상태. 각 항목에 정상(초록)/경고(노랑)/오류(빨강) 상태 인디케이터를 표시하고, 비정상 상태 시 상세 원인을 툴팁으로 표시한다. 60초 간격 자동 갱신한다. | P1 | Phase 1 | 1) 브라우저 인스턴스, 큐 상태, 외부 서비스, DB 연결 상태가 한 화면에 표시된다. 2) 각 항목에 정상/경고/오류 3단계 상태 인디케이터가 표시된다. 3) 경고 또는 오류 상태 시 대시보드 상단에 알림 배너가 표시된다. 4) 60초 간격으로 자동 갱신되며, 수동 새로고침 버튼도 제공된다. 5) 외부 서비스 잔여 크레딧이 임계치(20%) 이하 시 경고 상태로 전환된다. | Next.js API Routes (헬스체크 엔드포인트), TanStack Query (`refetchInterval: 60000`), shadcn/ui `Badge`, `Tooltip`, `Alert` |

---

## B. 사이트 관리 (Phase 1-2)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 | 기술 구현 참고 |
|----|------|------|---------|-------|----------|---------------|
| **FR-UI-005** | 사이트 목록 조회 (필터, 정렬, 페이지네이션) | `/sites` 페이지에서 등록된 모든 불법 도박 사이트 목록을 테이블 형태로 조회한다. 테이블 컬럼: 사이트명, URL, 카테고리(스포츠 도박/카지노/불법 경마/기타 도박), AI 분류 신뢰도, 위험도 점수, 상태(활성/비활성/폐쇄/모니터링중), 최근 채증일, 등록일. 서버사이드 필터링(카테고리, 상태, 위험도 범위, 탐지 출처), 서버사이드 정렬(모든 컬럼), 서버사이드 페이지네이션(페이지당 20/50/100건 선택)을 지원한다. 검색 바에서 URL, 도메인, 사이트명으로 텍스트 검색이 가능하며, URL 검색 파라미터로 필터 상태를 유지하여 공유 가능한 링크를 생성한다. | P0 | Phase 1 | 1) TanStack Table 기반 테이블에 8개 이상의 컬럼이 표시된다. 2) 카테고리, 상태, 위험도 범위 필터가 서버사이드로 동작한다. 3) 모든 컬럼에서 오름차순/내림차순 정렬이 가능하다. 4) 페이지네이션이 서버사이드로 동작하며, 페이지당 행 수를 선택할 수 있다. 5) 필터/정렬/페이지 상태가 URL 검색 파라미터에 반영되어 브라우저 뒤로가기/공유가 정상 동작한다. 6) 1,000건 이상의 데이터에서 테이블 렌더링 시간이 500ms 이내이다. | TanStack Table v8 (서버사이드 모드), `nuqs` (URL 상태 관리), shadcn/ui `Table`, `Select`, `Input`, Next.js `searchParams` |
| **FR-UI-006** | 사이트 상세 정보 페이지 | `/sites/[id]` 페이지에서 개별 사이트의 상세 정보를 종합적으로 표시한다. 표시 영역: (1) 기본 정보 — URL, 도메인, IP, 카테고리, 상태, AI 분류 결과(신뢰도 포함), 위험도 점수, 등록일, 탐지 출처, (2) WHOIS/DNS 정보 — 도메인 등록일, 만료일, 등록자, 네임서버, A/AAAA/MX 레코드, (3) 채증 이력 — 해당 사이트에 대한 모든 채증 세션 목록(날짜, 단계, 결과), 최근 채증 스크린샷 썸네일, (4) 도메인 변경 이력 — 도메인 호핑 타임라인(이전 도메인 → 현재 도메인), DNS 변경 이력, (5) 증거 파일 — 생성된 증거 패키지 목록 및 다운로드 링크, (6) 관련 사이트 — 동일 클러스터 소속 사이트 목록. 각 영역은 탭 또는 섹션으로 구분한다. | P0 | Phase 1 | 1) 6개 정보 영역이 탭 또는 섹션으로 구분되어 표시된다. 2) 채증 이력이 시간 역순 테이블로 표시되며, 각 행 클릭 시 채증 결과 상세 페이지로 이동한다. 3) 도메인 변경 이력이 타임라인 형태로 시각화된다. 4) 증거 파일 목록에서 개별/일괄 다운로드가 가능하다. 5) 관련 사이트(클러스터) 목록이 표시되고, 클릭 시 해당 사이트 상세 페이지로 이동한다. | Next.js 15 Parallel Routes (탭 구현), shadcn/ui `Tabs`, `Table`, `Timeline`(커스텀), TanStack Query (데이터 페칭) |
| **FR-UI-007** | 수동 URL 입력 및 채증 시작 | `/sites/new` 페이지에서 운영자 또는 수사관이 의심 URL을 수동으로 입력하고 채증을 시작할 수 있는 폼을 제공한다. 입력 필드: (1) 대상 URL (필수, URL 형식 검증), (2) 사이트명 (선택, 미입력 시 자동 추출 시도), (3) 카테고리 선택 (드롭다운, 선택 안함 시 AI 자동 분류), (4) 채증 옵션 — 즉시 채증 시작 / 예약 채증(날짜/시간 지정) / 큐에 등록만, (5) 채증 단계 선택 — 1단계만 / 1~2단계 / 1~3단계 전체, (6) 우선순위 (P1~P4), (7) 메모/사유 (선택). 폼 제출 시 URL 중복 체크를 수행하고, 중복 시 기존 사이트 링크와 함께 확인 다이얼로그를 표시한다. 폼 제출은 Server Action으로 처리하고, 성공 시 채증 작업 큐 페이지로 리디렉트한다. | P0 | Phase 1 | 1) URL 입력 시 실시간 형식 검증이 동작한다 (Zod 스키마). 2) URL 중복 체크가 폼 제출 전에 비동기로 수행된다. 3) 즉시 채증 선택 시 폼 제출 즉시 BullMQ 큐에 작업이 등록된다. 4) 예약 채증 선택 시 날짜/시간 피커가 표시되고, 해당 시각에 작업이 실행된다. 5) 폼 제출 성공 시 토스트 알림과 함께 채증 큐 페이지로 이동한다. 6) 서버 측 에러 시 인라인 에러 메시지가 표시된다. | React Hook Form + Zod, Next.js Server Actions, shadcn/ui `Form`, `Input`, `Select`, `DatePicker`, `RadioGroup`, `Textarea`, `toast` |
| **FR-UI-008** | 사이트 상태 관리 | 사이트 목록 및 상세 페이지에서 사이트의 운영 상태를 변경할 수 있는 기능을 제공한다. 상태 유형: (1) 활성(ACTIVE) — 현재 운영 중인 사이트, (2) 비활성(INACTIVE) — 일시 접속 불가 또는 점검 중, (3) 폐쇄(CLOSED) — 도메인 폐쇄/만료 확인, (4) 모니터링중(MONITORING) — 도메인 호핑 추적 대상. 상태 변경 시 변경 사유 입력을 필수로 하고, 변경 이력이 감사 로그에 기록된다. 폐쇄 상태 전환 시 동일 클러스터 사이트의 모니터링 주기를 자동 조정하는 알림을 표시한다. | P1 | Phase 1 | 1) 사이트 목록에서 다중 선택 후 일괄 상태 변경이 가능하다. 2) 상태 변경 시 변경 사유 입력 다이얼로그가 표시된다. 3) 상태 변경 이력이 사이트 상세 페이지의 활동 로그에 표시된다. 4) 상태 변경이 감사 로그(FR-EV-013)에 기록된다. 5) 폐쇄 상태 전환 시 동일 클러스터 사이트에 대한 모니터링 강화 알림이 표시된다. | shadcn/ui `Dialog`, `Select`, `AlertDialog`, Server Actions, PostgreSQL 상태 이력 테이블 |
| **FR-UI-009** | 벌크 URL 임포트 (CSV/엑셀 업로드) | `/sites/import` 페이지에서 다수의 URL을 CSV 또는 엑셀(XLSX) 파일로 일괄 업로드하여 사이트를 등록하고 채증 큐에 추가할 수 있다. 업로드 파일 형식: URL(필수), 사이트명(선택), 카테고리(선택), 메모(선택). 업로드 후 미리보기 테이블에 파싱 결과를 표시하고, URL 형식 검증, 중복 체크 결과를 행별로 표시한다. 유효한 항목만 선택하여 등록을 확인할 수 있다. 최대 1,000건까지 일괄 업로드를 지원한다. | P1 | Phase 2 | 1) CSV(.csv) 및 엑셀(.xlsx) 파일 드래그 앤 드롭 또는 파일 선택으로 업로드가 가능하다. 2) 업로드 후 파싱된 데이터가 미리보기 테이블에 표시된다. 3) 각 행에 URL 형식 유효성, 중복 여부가 시각적으로 표시된다 (초록 체크 / 빨강 X). 4) 유효/무효 항목 필터링 및 선택 등록이 가능하다. 5) 1,000건 업로드 시 파싱 및 검증이 10초 이내에 완료된다. | `papaparse` (CSV 파싱), `xlsx` (엑셀 파싱), shadcn/ui `Table`, `Checkbox`, `Button`, React Hook Form, Next.js Server Actions |
| **FR-UI-010** | 사이트 카테고리/태그 관리 | 사이트에 카테고리(불법 스포츠 도박, 불법 경마, 카지노, 기타 도박, 비도박) 및 사용자 정의 태그를 부여하고 관리할 수 있다. 카테고리는 AI 분류 결과를 기반으로 자동 부여되며, 운영자가 수동으로 변경할 수 있다. 태그는 자유 형식으로 추가/삭제가 가능하며(예: "최우선 감시", "도메인 호핑 이력", "법원 제출 완료" 등), 태그 기반 필터링을 지원한다. | P2 | Phase 2 | 1) 사이트 상세 페이지에서 카테고리 변경이 드롭다운으로 가능하다. 2) 태그 입력 시 자동완성(기존 태그 제안)이 동작한다. 3) 태그 추가/삭제가 실시간으로 반영된다. 4) 사이트 목록에서 태그 기반 필터링이 가능하다. 5) 카테고리/태그 변경 이력이 감사 로그에 기록된다. | shadcn/ui `Badge`, `Command` (태그 자동완성), `Popover`, TanStack Query (낙관적 업데이트) |

---

## C. 채증 모니터링 (Phase 1-2)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 | 기술 구현 참고 |
|----|------|------|---------|-------|----------|---------------|
| **FR-UI-011** | 채증 작업 큐 대시보드 | `/investigations` 페이지에서 전체 채증 작업 큐의 현황을 대시보드 형태로 표시한다. 상단에 상태별 요약 카드(대기/진행 중/완료/실패 건수)를 표시하고, 하단에 작업 목록 테이블을 제공한다. 테이블 컬럼: 작업 ID, 대상 URL, 채증 단계(1~3), 상태(대기/진행/완료/실패/수동개입필요), 우선순위(P1~P4), 등록 시각, 시작 시각, 완료 시각, 소요 시간, 실패 사유(실패 시). 상태별/우선순위별/기간별 필터링, 서버사이드 페이지네이션을 지원한다. 대기 중 작업의 우선순위를 수동 변경할 수 있고, 실패 작업의 재시도 버튼을 제공한다. | P0 | Phase 1 | 1) 4개 상태별 요약 카드(대기/진행/완료/실패)가 상단에 표시된다. 2) 작업 목록이 TanStack Table로 표시되며, 상태별 필터링이 동작한다. 3) 대기 중 작업의 우선순위를 드롭다운으로 변경할 수 있다. 4) 실패 작업에 "재시도" 버튼이 표시되며, 클릭 시 작업이 큐에 재등록된다. 5) 작업 상태가 SSE를 통해 실시간 업데이트된다. 6) 상태별 행 배경색이 구분된다 (진행 중: 파랑, 완료: 초록, 실패: 빨강). | TanStack Table v8, SSE (실시간 상태), shadcn/ui `Card`, `Table`, `Badge`, `Button`, `DropdownMenu`, BullMQ 이벤트 연동 |
| **FR-UI-012** | 개별 채증 세션 실시간 상세 모니터링 | `/investigations/[id]` 페이지에서 개별 채증 세션의 진행 상황을 실시간으로 상세 모니터링한다. 표시 영역: (1) 세션 기본 정보 — 대상 URL, 시작 시각, 현재 단계, 전체 진행률, (2) 단계별 진행 스테퍼 — 1단계(접속→팝업처리→스크린샷→HTML저장→WHOIS) → 2단계(회원가입폼탐지→번호입력→OTP인증→가입완료→배팅화면) → 3단계(배팅목록→배팅실행→확인) 각 스텝의 완료/진행/대기 상태 표시, (3) 실시간 로그 — 채증 과정의 상세 로그를 터미널 형태로 스트리밍, (4) 스크린샷 미리보기 — 각 단계에서 캡처된 스크린샷의 썸네일을 실시간 표시, (5) 에러 정보 — 에러 발생 시 에러 메시지, 에러 시점 스크린샷, 재시도 현황. WebSocket으로 실시간 데이터를 스트리밍한다. | P1 | Phase 2 | 1) 3단계 채증 파이프라인의 각 스텝이 스테퍼(Stepper) UI로 시각화된다. 2) 현재 진행 중인 스텝이 애니메이션(펄스)으로 강조된다. 3) 실시간 로그가 터미널 스타일로 자동 스크롤되며 표시된다. 4) 스크린샷이 캡처될 때마다 썸네일이 실시간으로 추가된다. 5) 에러 발생 시 에러 영역이 확장되어 상세 정보가 표시된다. | WebSocket (실시간 로그 스트리밍), shadcn/ui `Stepper`(커스텀), `ScrollArea`, `Accordion`, 이미지 최적화 (`next/image`), Zustand (세션 상태) |
| **FR-UI-013** | 채증 결과 갤러리 뷰 | `/investigations/gallery` 페이지에서 채증된 스크린샷을 갤러리(그리드) 형태로 조회한다. 각 스크린샷 카드에: (1) 썸네일 이미지, (2) 채증 단계(1/2/3) 배지, (3) 사이트 URL, (4) 채증 일시, (5) 채증 성공/실패 상태를 표시한다. 그리드 레이아웃(3~4열)과 리스트 레이아웃을 전환할 수 있고, 카테고리별/단계별/기간별 필터링을 지원한다. 스크린샷 클릭 시 라이트박스로 원본 해상도 이미지를 표시하며, 전체 화면 보기와 다운로드를 지원한다. | P1 | Phase 2 | 1) 스크린샷이 반응형 그리드(데스크톱 4열, 태블릿 2열, 모바일 1열)로 표시된다. 2) 그리드/리스트 뷰 전환이 가능하다. 3) 채증 단계, 카테고리, 기간 필터가 동작한다. 4) 스크린샷 클릭 시 라이트박스에서 원본 해상도로 확인할 수 있다. 5) 라이트박스에서 이전/다음 이미지 네비게이션(화살표 키 포함)이 가능하다. 6) 무한 스크롤 또는 페이지네이션으로 대량 이미지를 로드한다. | `next/image` (이미지 최적화, blur placeholder), shadcn/ui `Card`, `Dialog` (라이트박스), `ToggleGroup` (뷰 전환), TanStack Query `useInfiniteQuery` |
| **FR-UI-014** | 채증 결과 상세 페이지 | `/investigations/[id]/results` 페이지에서 완료된 채증 세션의 전체 결과를 상세하게 표시한다. 표시 영역: (1) 스크린샷 — 각 단계별 풀페이지 스크린샷을 전체 해상도로 조회, 이미지 줌인/줌아웃, 다운로드, (2) HTML 소스 — SingleFile HTML의 인라인 렌더링 미리보기(iframe) 또는 소스 코드 뷰어(코드 하이라이팅), (3) 네트워크 로그 — 페이지 로드 중 발생한 HTTP 요청 목록을 테이블로 표시(URL, 메서드, 상태코드, 크기, 타입), 도메인별 그루핑, 외부 도메인 하이라이팅, (4) WHOIS/DNS 정보 — 구조화된 형태로 표시(JSON 트리 뷰어), (5) 메타데이터 — 수집 시각, 접속 IP, 프록시 정보, 브라우저 정보, 소요 시간 등을 키-값 테이블로 표시, (6) 증거 파일 다운로드 — 개별 파일 및 전체 패키지 ZIP 다운로드. 각 영역은 탭으로 구분하여 표시한다. | P1 | Phase 2 | 1) 6개 탭(스크린샷/HTML/네트워크/WHOIS/메타데이터/다운로드)이 구분되어 표시된다. 2) 스크린샷 탭에서 이미지 줌인/줌아웃(마우스 휠 및 버튼)이 동작한다. 3) 네트워크 로그 탭에서 200건 이상의 요청 목록이 가상화 스크롤로 표시된다. 4) WHOIS/DNS 정보가 접이식 JSON 트리 뷰어로 표시된다. 5) 전체 증거 패키지 ZIP 다운로드가 동작한다. | shadcn/ui `Tabs`, `Table`, `ScrollArea`, `Collapsible`, `react-json-view` 또는 커스텀 JSON 트리, TanStack Virtual (네트워크 로그 가상화), Next.js API Routes (파일 다운로드) |

---

## D. 증거 관리 (Phase 1-2)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 | 기술 구현 참고 |
|----|------|------|---------|-------|----------|---------------|
| **FR-UI-015** | 증거 패키지 목록 및 다운로드 | `/evidence` 페이지에서 생성된 모든 증거 패키지를 목록으로 조회하고 다운로드할 수 있다. 테이블 컬럼: 패키지 ID, 사이트 URL, 채증 단계, 생성일시, 파일 수, 전체 크기, 무결성 상태(검증 완료/미검증/실패), 타임스탬프 상태(확인 완료/확인 대기중/실패). 필터링(무결성 상태, 기간), 정렬, 페이지네이션을 지원한다. 개별 패키지 ZIP 다운로드 및 다중 선택 일괄 다운로드를 지원하며, 다운로드 시 감사 로그에 다운로드 이벤트가 기록된다. | P0 | Phase 1 | 1) 증거 패키지 목록이 TanStack Table로 표시된다. 2) 무결성 상태별 필터링이 가능하다 (검증 완료/미검증/실패). 3) 개별 패키지 ZIP 다운로드가 동작한다. 4) 체크박스로 다중 선택 후 일괄 다운로드가 가능하다. 5) 다운로드 이벤트가 감사 로그(FR-EV-013)에 기록된다. | TanStack Table v8, Next.js API Routes (파일 스트리밍 다운로드), `archiver` (ZIP 생성), shadcn/ui `Table`, `Checkbox`, `Button` |
| **FR-UI-016** | 증거 무결성 검증 UI | `/evidence/[id]/verify` 페이지에서 증거 패키지의 무결성을 검증하고 결과를 시각적으로 표시한다. 검증 항목: (1) SHA-256 해시 검증 — 패키지 내 각 파일의 해시를 재계산하여 매니페스트와 비교, 파일별 일치/불일치 상태 표시, (2) OpenTimestamps 검증 — `.ots` 파일의 비트코인 블록체인 검증 결과, 블록 번호, 확인 시각 표시, (3) RFC 3161 검증 — TSA 타임스탬프 토큰의 디지털 서명 검증 결과, TSA 발급자 정보 표시, (4) 교차 검증 — 두 타임스탬프 간 시간 일관성 검증 결과. 각 검증 항목에 통과(초록 체크)/실패(빨강 X)/대기(회색) 아이콘을 표시하고, 실패 시 상세 원인을 표시한다. "지금 검증 실행" 버튼으로 온디맨드 검증을 수행할 수 있다. | P0 | Phase 1 | 1) 4가지 검증 항목(해시/OTS/RFC3161/교차검증)의 결과가 개별적으로 표시된다. 2) 해시 검증에서 파일별 일치/불일치 상태가 체크리스트 형태로 표시된다. 3) "지금 검증 실행" 버튼 클릭 시 실시간으로 검증이 수행되고 결과가 업데이트된다. 4) 모든 검증 통과 시 "무결성 검증 완료" 배지가 패키지에 부여된다. 5) 검증 실행 이벤트가 감사 로그에 기록된다. | Next.js Server Actions (검증 실행), shadcn/ui `Card`, `Badge`, `Progress`, `Alert`, Node.js `crypto` (해시 재계산), `opentimestamps` 클라이언트 |
| **FR-UI-017** | 증거 보고서 생성 및 미리보기 | `/evidence/[id]/report` 페이지에서 법원 제출용 증거 보고서를 생성하고 미리보기할 수 있다. 보고서 생성 플로우: (1) 보고서 템플릿 선택 (수사 착수용/법원 제출용/내부 보고용), (2) 포함할 증거 항목 선택 (스크린샷, WHOIS, 네트워크 로그 등), (3) Claude Sonnet 4.6이 한국어 보고서 본문을 자동 생성 (사이트 개요, 채증 과정, 증거 목록, 법적 분석), (4) 생성된 보고서 미리보기 (PDF 렌더링), (5) 수동 수정 가능한 에디터 제공, (6) PDF 다운로드 및 인쇄. 보고서에는 해시값, 타임스탬프 정보, 증거 체인(Chain of Custody) 요약이 자동 포함된다. | P2 | Phase 2 | 1) 3종 이상의 보고서 템플릿이 선택 가능하다. 2) Claude Sonnet 4.6이 한국어 보고서 본문을 자동 생성한다 (생성 시간 30초 이내). 3) 생성된 보고서가 PDF 미리보기로 표시된다. 4) 보고서 텍스트를 수동으로 편집할 수 있다. 5) PDF 다운로드 시 해시값, 타임스탬프, 스크린샷이 포함된다. | Claude Sonnet 4.6 API, `@react-pdf/renderer` 또는 Playwright `page.pdf()`, shadcn/ui `Select`, `Textarea`, `Button`, WYSIWYG 에디터 (Tiptap 또는 Lexical) |
| **FR-UI-018** | 증거 체인 오브 커스터디 감사 로그 뷰어 | `/evidence/[id]/audit` 페이지에서 증거 패키지의 전체 생명주기(Chain of Custody) 감사 로그를 타임라인 형태로 조회한다. 표시 항목: (1) 증거 수집 시작 시각/종료 시각, (2) 해시 생성 시각, (3) 타임스탬프 생성 시각(OTS + RFC 3161), (4) 패키지 생성 시각, (5) 모든 접근 이력 — 조회자 ID, 조회 시각, 조회 유형(열람/다운로드/검증/보고서 생성), (6) 상태 변경 이력 — 무결성 검증 결과 변경, 보고서 생성 등. 타임라인은 시간순 정렬이며, 각 이벤트에 아이콘, 시각(UTC), 수행자, 상세 설명을 표시한다. 필터링(이벤트 유형별)과 기간별 조회를 지원한다. | P1 | Phase 2 | 1) 감사 로그가 타임라인 형태로 시간순 표시된다. 2) 각 이벤트에 유형별 아이콘, UTC 시각, 수행자 정보가 표시된다. 3) 이벤트 유형별 필터링(수집/접근/검증/보고서)이 가능하다. 4) 기간별(시작일~종료일) 조회가 가능하다. 5) 감사 로그에 수정/삭제 기능이 없음이 UI에서 명시된다 (append-only 표시). | shadcn/ui `ScrollArea`, 커스텀 Timeline 컴포넌트, TanStack Table (이벤트 필터링), `DateRangePicker`, Lucide Icons (이벤트 유형별 아이콘) |

---

## E. CAPTCHA/OTP 수동 개입 (Phase 2)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 | 기술 구현 참고 |
|----|------|------|---------|-------|----------|---------------|
| **FR-UI-019** | CAPTCHA/OTP 수동 개입 큐 페이지 | `/investigations/captcha-queue` 페이지에서 자동 풀이가 실패하여 수동 개입이 필요한 CAPTCHA/OTP 세션의 대기 큐를 관리한다. 큐 목록에 표시할 항목: (1) 사이트 URL, (2) CAPTCHA/OTP 유형(reCAPTCHA/hCaptcha/Turnstile/이미지CAPTCHA/슬라이더/SMS OTP), (3) 대기 시간(실시간 카운터), (4) 채증 단계(1~3단계 중 어느 단계에서 발생), (5) 현재 페이지 스크린샷 미리보기 썸네일, (6) 자동 풀이 시도 이력(CapSolver→2Captcha 실패 로그). 대기 시간 기준 자동 정렬(오래된 것 우선)이며, 큐 항목 클릭 시 원격 브라우저 뷰어(FR-UI-020)가 열린다. 타임아웃 연장/건너뛰기/세션 종료 액션 버튼을 제공한다. 신규 큐 진입 시 브라우저 알림(Notification API)과 대시보드 상단 알림 배너를 표시한다. | P1 | Phase 2 | 1) 수동 개입 대기 큐가 대기 시간순(오래된 것 우선)으로 표시된다. 2) 각 항목에 CAPTCHA 유형, 사이트 URL, 대기 시간(실시간 카운터), 스크린샷 썸네일이 표시된다. 3) 큐 항목 클릭 시 원격 브라우저 뷰어(FR-UI-020)가 새 패널 또는 모달로 열린다. 4) 건너뛰기/타임아웃 연장(+5분)/세션 종료 버튼이 동작한다. 5) 신규 큐 진입 시 브라우저 알림(Notification API)이 발송된다. 6) 모바일에서도 터치 기반으로 조작 가능한 반응형 UI이다. | SSE 또는 WebSocket (실시간 큐 업데이트), Notification API, shadcn/ui `Card`, `Button`, `Badge`, `ScrollArea`, BullMQ 이벤트 연동 |
| **FR-UI-020** | CDP WebSocket 원격 브라우저 뷰어 | CAPTCHA/OTP 수동 풀이 시 서버에서 실행 중인 브라우저 세션의 화면을 대시보드에 실시간 스트리밍하는 원격 브라우저 뷰어를 제공한다. 기능: (1) 브라우저 화면 실시간 스트리밍 — CDP(Chrome DevTools Protocol) WebSocket을 통해 `Page.screencastFrame` 이벤트로 화면을 초당 5~10프레임으로 수신하여 `<canvas>` 또는 `<img>` 요소에 렌더링, (2) 원격 입력 — 사용자의 마우스 클릭/이동/스크롤 및 키보드 입력을 CDP `Input.dispatchMouseEvent`/`Input.dispatchKeyEvent`로 원격 브라우저에 전달, (3) CAPTCHA 풀이 완료 감지 — 페이지 DOM 변화 또는 URL 변경을 감지하여 풀이 완료 시 자동으로 채증 자동화를 재개하고 뷰어를 닫음, (4) 상태 표시 — 연결 상태(연결됨/연결 중/연결 끊김), 남은 타임아웃 시간. | P1 | Phase 2 | 1) 원격 브라우저 화면이 대시보드 내 뷰어에 실시간 스트리밍된다 (지연 2초 이내). 2) 마우스 클릭/이동이 원격 브라우저에 정상 전달된다. 3) 키보드 입력이 원격 브라우저에 정상 전달된다. 4) CAPTCHA 풀이 완료 시 자동으로 채증이 재개되고 뷰어가 닫힌다. 5) 연결 끊김 시 자동 재연결을 3회 시도하고, 실패 시 에러 표시. 6) 남은 타임아웃 시간이 카운트다운으로 표시된다. | CDP WebSocket (`--remote-debugging-port=9222`), `Page.startScreencast` / `Page.screencastFrame`, `Input.dispatchMouseEvent` / `Input.dispatchKeyEvent`, HTML5 Canvas 렌더링, WebSocket 클라이언트 |
| **FR-UI-021** | Slack/웹 푸시 알림 설정 | 사용자별로 알림 채널 및 알림 유형을 설정할 수 있는 알림 설정 페이지를 제공한다. 알림 채널: (1) Slack — Slack Incoming Webhook URL 등록, 특정 Slack 채널 지정, (2) 웹 푸시 — 브라우저 Service Worker 기반 푸시 알림 등록/해제, (3) 대시보드 내 알림 — 대시보드 상단 알림 벨 아이콘, 알림 드롭다운. 알림 유형별 활성/비활성 설정: 수동 개입 요청, 채증 완료, 채증 실패, 시스템 오류, 일일 요약. 근무 시간 설정(예: 09:00~18:00)으로 근무 외 시간 알림 억제 옵션을 제공한다. | P1 | Phase 2 | 1) 사용자가 Slack Webhook URL을 등록하고 테스트 메시지를 전송할 수 있다. 2) 웹 푸시 알림 구독/해제가 동작한다. 3) 알림 유형(5종)별로 개별 활성/비활성 설정이 가능하다. 4) 근무 시간 설정이 가능하며, 근무 외 시간에는 설정에 따라 알림이 억제된다. 5) 대시보드 내 알림 벨 아이콘에 미확인 알림 수가 배지로 표시된다. | Slack Incoming Webhook API, Web Push API + Service Worker (`PushManager`), shadcn/ui `Switch`, `Input`, `DropdownMenu`, `Badge`, Zustand (알림 상태) |

---

## F. AI 분류 검토 (Phase 2)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 | 기술 구현 참고 |
|----|------|------|---------|-------|----------|---------------|
| **FR-UI-022** | 저신뢰 분류 결과 수동 검토 큐 | `/review` 페이지에서 AI 분류 신뢰도가 임계값(0.7) 미만인 사이트의 분류 결과를 수동으로 검토하는 큐를 관리한다. 큐 목록에 표시할 항목: (1) 사이트 URL, (2) AI 분류 결과(카테고리 + 신뢰도 점수), (3) 대표 스크린샷 썸네일, (4) 등록 일시, (5) 검토 상태(대기/검토 중/완료). 신뢰도 점수가 낮은 순으로 기본 정렬하며, 카테고리별 필터링을 지원한다. 검토 대기 건수가 대시보드 사이드바의 "AI 검토" 메뉴에 배지로 표시된다. 큐 항목 클릭 시 AI 분류 상세 검토 페이지(FR-UI-023)로 이동한다. | P1 | Phase 2 | 1) 신뢰도 0.7 미만 사이트가 자동으로 검토 큐에 표시된다. 2) 신뢰도 점수 오름차순(낮은 순)으로 기본 정렬된다. 3) 카테고리별, 기간별 필터링이 가능하다. 4) 대시보드 사이드바 메뉴에 검토 대기 건수가 배지로 표시된다. 5) 큐 항목 클릭 시 상세 검토 페이지로 이동한다. 6) 검토 완료 시 큐에서 자동 제거된다. | TanStack Table v8, shadcn/ui `Table`, `Badge`, `Card`, Next.js Parallel Routes (사이드바 배지 업데이트), TanStack Query |
| **FR-UI-023** | AI 분류 상세 페이지 (분류 근거, 승인/수정/반려) | `/review/[id]` 페이지에서 개별 사이트의 AI 분류 결과를 상세 검토하고 승인/수정/반려 액션을 수행한다. 표시 영역: (1) 사이트 정보 — URL, 도메인, 대표 스크린샷(풀사이즈 뷰어), (2) AI 분류 결과 — 카테고리, 신뢰도 점수(0~1, 프로그레스 바로 시각화), 분류 근거(evidence 배열, Claude 응답의 각 근거 항목을 불릿 리스트로 표시), (3) 검토자 액션 — 승인(AI 분류 결과 확정) / 수정(카테고리를 수동으로 변경, 변경 사유 입력 필수) / 반려(비도박으로 재분류, 반려 사유 입력 필수). 승인/수정/반려 시 결과가 DB에 반영되고, 학습 데이터 테이블에 자동 기록된다(향후 ML 모델 학습용). 키보드 단축키: `A`(승인), `E`(수정), `R`(반려), `→`(다음 건)을 지원하여 빠른 검토를 돕는다. | P1 | Phase 2 | 1) AI 분류 카테고리, 신뢰도 점수, 분류 근거가 구조화되어 표시된다. 2) 승인/수정/반려 버튼이 명확하게 구분되어 표시된다. 3) 수정/반려 시 사유 입력 다이얼로그가 표시되며, 사유 미입력 시 제출이 차단된다. 4) 검토 결과가 학습 데이터 테이블에 자동 기록된다. 5) 키보드 단축키(A/E/R/→)로 빠른 검토가 가능하다. 6) 검토 완료 후 자동으로 다음 검토 대기 건으로 이동한다. | shadcn/ui `Button`, `Dialog`, `Progress`, `Textarea`, React Hook Form + Zod, `useHotkeys` 커스텀 훅, Server Actions, Next.js `router.push()` |

---

## G. 통계 및 리포트 (Phase 3-4)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 | 기술 구현 참고 |
|----|------|------|---------|-------|----------|---------------|
| **FR-UI-024** | 통계 대시보드 (기간별 탐지/채증 추이 차트) | `/analytics` 페이지에서 불법 도박 사이트 탐지 및 채증 현황의 기간별 추이를 차트로 시각화한다. 차트 유형: (1) 일별/주별/월별 신규 탐지 사이트 수 추이 (라인 차트), (2) 일별/주별/월별 채증 성공/실패 건수 추이 (스택 영역 차트), (3) 채증 성공률 추이 (라인 차트, 목표선 70% 표시), (4) 평균 채증 소요 시간 추이 (라인 차트). 기간 범위 선택기(최근 7일/30일/90일/1년/커스텀)를 제공하고, 차트 데이터를 CSV로 내보내기할 수 있다. 반응형으로 모바일에서도 차트가 정상 표시된다. | P2 | Phase 3 | 1) 4종 이상의 추이 차트가 `/analytics` 페이지에 표시된다. 2) 기간 범위 선택기(7일/30일/90일/1년/커스텀)로 차트 데이터가 갱신된다. 3) 차트에 마우스 호버 시 해당 날짜의 상세 수치가 툴팁으로 표시된다. 4) 차트 데이터를 CSV 파일로 다운로드할 수 있다. 5) 모바일 뷰포트(375px)에서 차트가 정상 렌더링된다. | Recharts (`LineChart`, `AreaChart`), shadcn/ui `DateRangePicker`, `Select`, `Button`, TanStack Query (기간별 데이터 페칭), `papaparse` (CSV 내보내기) |
| **FR-UI-025** | 카테고리별 사이트 분포 차트 | `/analytics` 페이지 내에서 탐지된 불법 도박 사이트의 카테고리별 분포를 시각화한다. 차트 유형: (1) 카테고리별 사이트 수 파이 차트 (불법 스포츠 도박/불법 경마/카지노/기타 도박/비도박), (2) 카테고리별 사이트 수 수평 바 차트 (수치 레이블 포함), (3) 카테고리별 채증 성공률 비교 바 차트, (4) 상태별(활성/비활성/폐쇄/모니터링중) 분포 도넛 차트. 각 차트의 세그먼트 클릭 시 해당 카테고리/상태로 필터링된 사이트 목록 페이지로 이동한다. | P2 | Phase 3 | 1) 카테고리별 사이트 수 파이 차트가 표시된다. 2) 상태별 분포 도넛 차트가 표시된다. 3) 차트 세그먼트 클릭 시 해당 필터가 적용된 `/sites` 페이지로 이동한다. 4) 각 세그먼트에 수량 및 비율(%)이 레이블로 표시된다. 5) 차트 범례(Legend)가 표시되고, 범례 클릭으로 세그먼트 토글이 가능하다. | Recharts (`PieChart`, `BarChart`), shadcn/ui 레이아웃 컴포넌트, Next.js `router.push()` (차트 클릭 네비게이션) |
| **FR-UI-026** | 도메인 호핑 네트워크 시각화 | `/analytics` 페이지 또는 별도 `/analytics/network` 페이지에서 도메인 호핑 관계 및 사이트 간 연결 네트워크를 인터랙티브 그래프로 시각화한다. 그래프 요소: (1) 노드 — 각 사이트 도메인 (크기: 연결 수에 비례, 색상: 카테고리별, 아이콘: 상태별), (2) 엣지 — 도메인 호핑 관계(실선, 방향 화살표), 링크 참조 관계(점선), 동일 운영자 추정 관계(굵은 선), (3) 클러스터 — 동일 운영자 그룹을 시각적으로 묶어 표시. 인터랙션: 노드 드래그, 줌인/줌아웃, 노드 클릭 시 사이트 정보 패널 표시, 특정 클러스터 하이라이팅, 시간 범위 슬라이더(특정 기간의 네트워크 변화 재생). | P2 | Phase 3 | 1) 도메인 간 관계가 방향 그래프로 시각화된다. 2) 노드 크기/색상이 연결 수/카테고리에 따라 차별화된다. 3) 줌인/줌아웃 및 노드 드래그가 동작한다. 4) 노드 클릭 시 사이트 상세 정보 패널이 표시된다. 5) 100개 이상의 노드에서 렌더링 성능이 유지된다 (30fps 이상). | D3.js (force-directed graph) 또는 Cytoscape.js, WebGL 렌더링(대규모 노드), shadcn/ui `Sheet` (사이드 패널), `Slider` (시간 범위) |
| **FR-UI-027** | 월간/주간 정기 보고서 자동 생성 | `/reports` 페이지에서 월간/주간 정기 보고서를 자동 생성하고 관리한다. 보고서 포함 내용: (1) 기간 내 신규 탐지 사이트 수 및 카테고리별 분류, (2) 채증 성공/실패 통계 및 성공률 추이, (3) 도메인 호핑 현황 (변경 감지 건수, 신규 도메인), (4) 주요 적발 사이트 요약(상위 10건, 스크린샷 썸네일 포함), (5) 외부 서비스 사용량 및 비용 요약, (6) AI 분석 인사이트(Claude Sonnet 4.6 기반 동향 분석, 특이사항). 보고서 생성 주기를 크론 스케줄로 설정하고, 생성 완료 시 이메일/Slack으로 자동 발송한다. 보고서 이력을 관리하여 기간 간 비교(전월 대비 증감률)를 제공한다. | P2 | Phase 3 | 1) 주간/월간 보고서가 크론 스케줄에 따라 자동 생성된다. 2) 보고서에 6개 섹션(탐지 통계, 채증 통계, 도메인 호핑, 주요 사이트, 비용, AI 인사이트)이 포함된다. 3) 생성 완료 시 Slack/이메일로 자동 발송된다. 4) 이전 보고서 목록이 날짜순으로 조회 가능하다. 5) 전월/전주 대비 증감률이 자동 산출되어 보고서에 포함된다. | BullMQ (크론 스케줄), Claude Sonnet 4.6 API (동향 분석), Slack Webhook / Nodemailer (발송), `@react-pdf/renderer` (PDF 생성), PostgreSQL (보고서 이력) |
| **FR-UI-028** | 보고서 내보내기 (PDF, Excel) | 생성된 보고서 및 통계 데이터를 PDF, Excel(XLSX) 형식으로 내보내기할 수 있다. PDF 내보내기: 보고서 전체를 A4 형식 PDF로 생성하며, 차트/표/스크린샷 이미지가 포함된다. Excel 내보내기: 통계 데이터를 시트별로 구조화하여 XLSX 파일로 생성하며, 시트 구성은 (1) 요약, (2) 사이트 목록, (3) 채증 현황, (4) 카테고리별 통계, (5) 월별 추이. 기관별 양식 템플릿(사행산업통합감독위원회, 경찰청, 한국마사회)을 선택할 수 있다. | P2 | Phase 3 | 1) 보고서를 PDF 형식으로 다운로드할 수 있다. 2) 통계 데이터를 XLSX 형식으로 다운로드할 수 있으며, 5개 시트로 구성된다. 3) PDF에 차트 이미지와 표가 정상 포함된다. 4) 기관별 양식 템플릿(최소 3종)을 선택할 수 있다. 5) 1,000건 이상의 데이터 포함 시 내보내기가 30초 이내에 완료된다. | `@react-pdf/renderer` (PDF), `exceljs` 또는 `xlsx` (엑셀), Recharts `Recharts.render()` (차트 → 이미지 변환), Next.js API Routes (파일 생성/다운로드) |

---

## H. 시스템 설정 (Phase 1-2)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 | 기술 구현 참고 |
|----|------|------|---------|-------|----------|---------------|
| **FR-UI-029** | 사용자/역할 관리 | `/settings/users` 페이지에서 시스템 사용자 계정을 생성, 수정, 비활성화하고 역할 기반 권한을 관리한다. 역할 체계: (1) ADMIN(관리자) — 전체 시스템 설정, 사용자 관리, 증거 삭제 권한, (2) OPERATOR(운영자) — 키워드 관리, 채증 실행, 큐 관리, 수동 개입, (3) INVESTIGATOR(수사관) — 채증 결과 열람, 증거 다운로드, 수동 URL 등록, (4) VIEWER(뷰어) — 읽기 전용, 통계/보고서 열람. 사용자 목록 테이블에 이름, 이메일, 역할, 상태(활성/비활성), 최종 로그인 일시를 표시하고, 사용자 생성/편집 폼에서 역할 할당과 비밀번호 초기화를 수행한다. 모든 사용자 관리 작업이 감사 로그에 기록된다. | P0 | Phase 1 | 1) 4개 역할(ADMIN/OPERATOR/INVESTIGATOR/VIEWER)이 정의되고 권한 매트릭스가 적용된다. 2) 사용자 목록에서 생성/수정/비활성화가 가능하다. 3) 역할별 메뉴 접근 제어가 동작한다 (예: VIEWER는 설정 메뉴 접근 불가). 4) 비밀번호 초기화 시 이메일로 재설정 링크가 발송된다. 5) 모든 사용자 관리 작업이 감사 로그에 기록된다. | NextAuth.js v5 (인증 + 세션), PostgreSQL (사용자/역할 테이블), shadcn/ui `Table`, `Dialog`, `Form`, `Select`, 미들웨어 기반 RBAC |
| **FR-UI-030** | 시스템 설정 페이지 | `/settings` 페이지에서 시스템 전반의 설정을 관리한다. 설정 카테고리: (1) 외부 서비스 API 키 — Claude API Key, CapSolver API Key, 2Captcha API Key, PVAPins API Key, GrizzlySMS API Key, Google Custom Search API Key/CX, (2) 프록시 설정 — 프록시 서비스 URL/인증 정보, 로테이션 주기, 국가 설정(한국 우선), (3) 채증 설정 — 동시 브라우저 인스턴스 수(기본 5, 최대 20), 채증 타임아웃(1단계/2단계/3단계별), 재시도 횟수, (4) 알림 설정 — 글로벌 Slack Webhook URL, 알림 임계치(실패율, API 할당량), (5) 스토리지 설정 — 증거 파일 저장 경로, 보존 기간, 최대 용량 경고 임계치. API 키는 마스킹 처리되어 표시되고, 변경 시 확인 다이얼로그를 표시한다. 설정 변경 시 감사 로그에 기록되며, 이전 값은 복원 가능하다. | P1 | Phase 1 | 1) 5개 설정 카테고리가 탭 또는 섹션으로 구분되어 표시된다. 2) API 키 입력 필드에 마스킹(****) 처리가 적용되고, "보기" 토글로 확인 가능하다. 3) 설정 변경 시 확인 다이얼로그가 표시되며, 저장 성공 시 토스트 알림이 표시된다. 4) 설정 변경 이력이 감사 로그에 기록된다 (변경 항목, 이전 값 해시, 변경자). 5) API 키 연결 테스트("Test Connection") 버튼이 각 서비스에 제공된다. | React Hook Form + Zod, shadcn/ui `Tabs`, `Form`, `Input`(type=password), `Switch`, `Button`, `toast`, Server Actions, 환경 변수 또는 암호화 DB 저장 |
| **FR-UI-031** | 키워드 관리 페이지 | `/settings/keywords` 페이지에서 불법 도박 사이트 탐지용 키워드를 관리한다. 기능: (1) 키워드 CRUD — 키워드 추가, 수정, 삭제, 활성화/비활성화, (2) 카테고리별 분류 — 스포츠 도박, 카지노, 경마, 파워볼 등 카테고리별 키워드 그루핑, (3) 키워드 조합 관리 — AND/NOT 조합 템플릿 정의 및 편집, (4) 키워드 통계 — 키워드별 탐지 건수, 정밀도(불법 사이트 확인 비율), 최근 사용일, (5) 시드 키워드 자동 확장 — 시드 키워드 입력 시 Claude Haiku가 유사 키워드를 10개 이상 자동 생성, 생성 결과를 검토·선택하여 등록. 키워드 변경 이력이 감사 로그에 기록된다. | P1 | Phase 2 | 1) 키워드 목록이 카테고리별로 그루핑되어 표시된다. 2) 키워드 추가/수정/삭제/활성화 토글이 동작한다. 3) 키워드별 탐지 건수, 정밀도 통계가 표시된다. 4) 시드 키워드 입력 시 Claude Haiku가 유사 키워드를 자동 생성하고, 결과를 체크박스로 선택하여 일괄 등록할 수 있다. 5) 키워드 변경 이력이 감사 로그에 기록된다. | shadcn/ui `Table`, `Input`, `Switch`, `Badge`, `Dialog`, TanStack Table (카테고리별 그루핑), Claude Haiku 4.5 API (키워드 자동 생성), Server Actions |
| **FR-UI-032** | 감사 로그 뷰어 (시스템 전체) | `/settings/audit-log` 페이지에서 시스템 전체의 감사 로그를 조회한다. 로그 항목: (1) 타임스탬프(UTC), (2) 수행자 ID 및 역할, (3) 이벤트 유형(로그인/로그아웃/사이트등록/채증실행/증거접근/설정변경/사용자관리 등), (4) 대상 리소스(사이트 ID, 증거 패키지 ID 등), (5) 변경 내용 요약, (6) IP 주소. 필터링: 이벤트 유형별, 수행자별, 기간별, 리소스별. 검색: 키워드 검색(대상 URL, 수행자명 등). 서버사이드 페이지네이션(100건/페이지)을 지원하며, 감사 로그는 읽기 전용으로 수정/삭제 UI가 없다. CSV 내보내기를 지원한다. | P1 | Phase 1 | 1) 감사 로그가 시간 역순으로 테이블 형태로 표시된다. 2) 이벤트 유형별, 수행자별, 기간별 필터링이 동작한다. 3) 키워드 검색이 동작한다. 4) 서버사이드 페이지네이션(100건/페이지)이 동작한다. 5) 수정/삭제 버튼이 존재하지 않으며, 읽기 전용임이 UI에 명시된다. 6) CSV 내보내기가 동작한다. | TanStack Table v8 (서버사이드), shadcn/ui `Table`, `Select`, `Input`, `DateRangePicker`, `Button`, PostgreSQL (append-only 감사 테이블), `papaparse` (CSV) |

---

## I. 반응형 및 접근성 (Phase 1)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 | 기술 구현 참고 |
|----|------|------|---------|-------|----------|---------------|
| **FR-UI-033** | 반응형 레이아웃 | 대시보드 전체 레이아웃이 데스크톱(1920px), 태블릿(768px), 모바일(375px) 뷰포트에서 정상적으로 표시되고 조작 가능하도록 반응형 디자인을 적용한다. 레이아웃 구성: (1) 데스크톱 — 좌측 사이드바(240px) + 메인 콘텐츠, 사이드바 접기/펼치기 토글, (2) 태블릿 — 사이드바 오버레이(햄버거 메뉴), 테이블 컬럼 축소(주요 컬럼만 표시, 상세는 확장 행으로), (3) 모바일 — 하단 네비게이션 바, 카드 기반 리스트 뷰(테이블 대체), 풀스크린 모달. 터치 인터랙션(스와이프, 탭 앤 홀드) 및 터치 타겟 크기(최소 44x44px)를 보장한다. | P0 | Phase 1 | 1) 데스크톱(1920px) 뷰포트에서 사이드바 + 메인 콘텐츠 레이아웃이 정상 표시된다. 2) 태블릿(768px) 뷰포트에서 사이드바가 오버레이로 전환되고, 테이블이 축소 표시된다. 3) 모바일(375px) 뷰포트에서 하단 네비게이션 바가 표시되고, 카드 기반 리스트가 동작한다. 4) 모든 클릭 가능 요소의 터치 타겟이 최소 44x44px이다. 5) 가로/세로 방향 전환(orientation change) 시 레이아웃이 정상 재배치된다. | TailwindCSS v4 반응형 유틸리티(`sm:`, `md:`, `lg:`), shadcn/ui `Sheet` (모바일 사이드바), `NavigationMenu`, CSS Container Queries (선택적) |
| **FR-UI-034** | 다크 모드 지원 | 대시보드 전체에 다크 모드를 지원한다. 모드 전환: (1) 시스템 설정 따르기(기본값, `prefers-color-scheme` 미디어 쿼리), (2) 수동 전환(라이트/다크/시스템), 사용자 선택을 로컬 스토리지에 저장하여 세션 간 유지. 다크 모드에서: 배경색, 텍스트 색상, 카드 색상, 테이블 행 색상, 차트 색상, 상태 배지 색상이 모두 다크 테마에 맞게 조정된다. 색상 대비비(contrast ratio)는 WCAG 2.1 AA 기준(일반 텍스트 4.5:1, 대형 텍스트 3:1)을 충족한다. | P1 | Phase 1 | 1) 헤더 또는 사이드바에 라이트/다크/시스템 모드 전환 토글이 제공된다. 2) 다크 모드에서 모든 UI 요소(카드, 테이블, 차트, 배지 등)의 색상이 적절히 전환된다. 3) 사용자의 모드 선택이 로컬 스토리지에 저장되어 새로고침 후에도 유지된다. 4) 시스템 모드 선택 시 OS의 다크 모드 설정을 따른다. 5) 다크 모드에서 텍스트 대비비가 WCAG 2.1 AA 기준을 충족한다. | TailwindCSS v4 다크 모드(`dark:` 접두사, `class` 전략), `next-themes` (테마 관리), shadcn/ui 내장 다크 모드 지원, CSS custom properties |
| **FR-UI-035** | 로딩/에러 상태 처리 | 대시보드 전체에 일관된 로딩 상태 및 에러 상태 처리를 적용한다. 로딩 상태: (1) 페이지 전환 시 상단 프로그레스 바(NProgress 스타일), (2) 데이터 로딩 시 스켈레톤 UI(테이블 스켈레톤, 카드 스켈레톤, 차트 스켈레톤), (3) 버튼 클릭 후 서버 응답 대기 시 로딩 스피너 + 버튼 비활성화. 에러 상태: (1) React Error Boundary로 컴포넌트 단위 에러 격리, 에러 발생 시 "문제가 발생했습니다" 폴백 UI + 재시도 버튼, (2) API 에러(4xx/5xx) 시 인라인 에러 메시지 + 토스트 알림, (3) 네트워크 연결 끊김 시 오프라인 배너 표시, (4) 404 페이지 — 커스텀 Not Found 페이지. 모든 에러는 클라이언트 사이드 로깅 서비스(선택적)에 자동 보고된다. | P0 | Phase 1 | 1) 페이지 전환 시 상단 프로그레스 바가 표시된다. 2) 데이터 로딩 중 스켈레톤 UI가 콘텐츠 영역에 표시된다. 3) 컴포넌트 에러 시 Error Boundary가 동작하여 전체 페이지 크래시가 방지된다. 4) API 에러 시 토스트 알림 + 인라인 에러 메시지가 표시된다. 5) 네트워크 연결 끊김 시 오프라인 배너가 상단에 표시된다. 6) 커스텀 404 Not Found 페이지가 제공된다. | Next.js 15 `loading.tsx` (스트리밍 SSR), `error.tsx` (Error Boundary), `not-found.tsx`, shadcn/ui `Skeleton`, `Sonner` (토스트), `nprogress` (페이지 전환 프로그레스), Zustand (네트워크 상태 관리) |

---

## 요구사항 요약

### 우선순위별 분류

| 우선순위 | 요구사항 수 | 해당 ID | Phase |
|---------|-----------|---------|-------|
| **P0** | 8건 | FR-UI-001, 002, 005, 006, 007, 011, 015, 016, 029, 032, 033, 035 | Phase 1 |
| **P1** | 18건 | FR-UI-003, 004, 008, 012, 013, 014, 018, 019, 020, 021, 022, 023, 030, 031, 034, 036, 037 | Phase 1-2 |
| **P2** | 12건 | FR-UI-009, 010, 017, 024, 025, 026, 027, 028, 038 | Phase 2-3 |

### Phase별 분류

| Phase | 핵심 기능 | 요구사항 |
|-------|----------|---------|
| **Phase 1** (MVP) | 메인 대시보드, 사이트 목록/상세/등록, 채증 큐, 증거 목록/검증, 사용자 관리, 감사 로그, 반응형, 로딩/에러 처리 | FR-UI-001, 002, 003, 004, 005, 006, 007, 008, 011, 015, 016, 029, 030, 032, 033, 034, 035 |
| **Phase 2** (자동 채증) | 랜딩 페이지, 채증 세션 모니터링, 결과 갤러리/상세, 증거 보고서, 감사 로그 뷰어, CAPTCHA 큐, 원격 브라우저, 알림 설정, AI 검토 큐, 벌크 임포트, 카테고리/태그 관리, 키워드 관리 | FR-UI-009, 010, 012, 013, 014, 017, 018, 019, 020, 021, 022, 023, 031, 036, 037, 038 |
| **Phase 3** (AI 탐지) | 통계 대시보드, 카테고리 차트, 네트워크 시각화, 정기 보고서, 내보내기 | FR-UI-024, 025, 026, 027, 028 |

### 기술 의존성 맵

```
Phase 1 (기반):
  Next.js 15 + shadcn/ui → 전체 UI 프레임워크
  NextAuth.js v5 → FR-UI-029 (사용자 관리) → 전체 RBAC
  TanStack Table → FR-UI-005 (사이트 목록), FR-UI-011 (채증 큐), FR-UI-015 (증거 목록), FR-UI-032 (감사 로그)
  TanStack Query → 전체 서버 상태 관리
  SSE → FR-UI-002 (활동 피드), FR-UI-003 (채증 모니터링)

Phase 2 (확장):
  WebSocket → FR-UI-012 (채증 세션 상세), FR-UI-020 (원격 브라우저)
  CDP WebSocket → FR-UI-020 (원격 브라우저 뷰어)
  Web Push API → FR-UI-021 (알림 설정)
  Claude Sonnet 4.6 → FR-UI-017 (증거 보고서)
  Claude Haiku 4.5 → FR-UI-031 (키워드 자동 생성)

Phase 3 (시각화):
  Recharts → FR-UI-024 (추이 차트), FR-UI-025 (분포 차트)
  D3.js / Cytoscape.js → FR-UI-026 (네트워크 그래프)
  @react-pdf/renderer → FR-UI-027 (보고서 PDF), FR-UI-028 (내보내기)
  exceljs → FR-UI-028 (엑셀 내보내기)
```

### 역할별 접근 권한 매트릭스

| 기능 영역 | ADMIN | OPERATOR | INVESTIGATOR | VIEWER |
|----------|-------|----------|-------------|--------|
| 메인 대시보드 (FR-UI-001~004) | 전체 | 전체 | 읽기 | 읽기 |
| 사이트 관리 (FR-UI-005~010) | 전체 | 생성/수정/조회 | 조회/URL 등록 | 조회 |
| 채증 모니터링 (FR-UI-011~014) | 전체 | 전체 | 조회 | 조회 |
| 증거 관리 (FR-UI-015~018) | 전체 | 조회/다운로드 | 조회/다운로드 | 조회 |
| CAPTCHA/OTP 개입 (FR-UI-019~021) | 전체 | 전체 | 수동 풀이 | 접근 불가 |
| AI 분류 검토 (FR-UI-022~023) | 전체 | 전체 | 조회 | 접근 불가 |
| 통계/리포트 (FR-UI-024~028) | 전체 | 조회/내보내기 | 조회/내보내기 | 조회 |
| 시스템 설정 (FR-UI-029~032) | 전체 | 키워드 관리만 | 접근 불가 | 접근 불가 |

---

## 개선 UI/UX 요구사항 (v1.3 추가)

> 추가일: 2026-03-18
> 시나리오 분석을 통해 도출된 UI/UX 개선 요구사항

### 원클릭 채증 (UX-1)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 |
|----|------|------|---------|-------|----------|
| FR-UI-039 | 빠른 URL 입력 바 | 대시보드 상단에 "빠른 URL 입력" 바를 배치하여, URL 붙여넣기만으로 사이트 등록 + 1단계 채증이 자동 시작된다. 긴급 채증 시 5단계 절차를 1단계로 단축한다. | P1 | Phase 2 | 1. 대시보드 상단에 URL 입력 바가 항상 표시된다 2. URL 붙여넣기 후 Enter 시 사이트 자동 등록 + 1단계 채증 큐 등록된다 3. 중복 URL 입력 시 기존 사이트 상세 페이지로 이동한다 4. 키보드 단축키 Ctrl+N으로 입력 바에 포커스된다 |
| FR-UI-040 | 사이트 목록 즉시 채증 버튼 | 사이트 목록 테이블의 각 행에 "즉시 채증" 아이콘 버튼을 추가하여, 클릭 시 기본 설정으로 즉시 채증 큐에 등록한다. Dialog 없이 원클릭으로 채증을 시작할 수 있다. | P1 | Phase 2 | 1. 사이트 목록 각 행에 ▶️ 아이콘 버튼이 표시된다 2. 클릭 시 확인 없이 기본 설정(전체 3단계)으로 채증이 시작된다 3. 채증 시작 시 토스트 알림이 표시된다 4. 이미 채증 진행 중인 사이트는 버튼이 비활성화된다 |

---

### Kanban 채증 모니터링 (UX-2)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 |
|----|------|------|---------|-------|----------|
| FR-UI-041 | Kanban 스타일 채증 보드 | 채증 모니터링 페이지에 Kanban 보드 뷰를 추가한다. 대기/1단계/2단계/3단계/완료/실패 컬럼으로 카드가 실시간 이동하며, 각 카드에 미니 스크린샷 썸네일과 프로그레스 바가 표시된다. | P1 | Phase 2 | 1. 6개 컬럼(대기/1단계/2단계/3단계/완료/실패)으로 구성된다 2. SSE 기반으로 카드가 실시간 이동한다 3. 각 카드에 사이트 URL, 미니 스크린샷, 경과 시간이 표시된다 4. 실패 카드 클릭 시 에러 메시지와 실패 시점 스크린샷이 즉시 확인된다 5. 테이블 뷰와 Kanban 뷰를 토글로 전환할 수 있다 |

---

### 모바일/Slack 수동 개입 최적화 (UX-4)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 |
|----|------|------|---------|-------|----------|
| FR-UI-042 | 모바일 전용 수동 개입 UI | CAPTCHA 이미지만 표시 + 텍스트 입력 필드의 경량 모바일 UI를 제공한다. CDP 스트리밍 대신 이미지 기반으로 모바일에서도 원활하게 수동 개입이 가능하다. | P2 | Phase 3 | 1. 모바일 화면에 CAPTCHA 이미지와 입력 필드만 표시된다 2. OTP 코드 직접 입력 모드가 지원된다 3. 터치 기반 조작이 원활하다 4. 응답 시간 5분 → 2분 목표 |
| FR-UI-043 | Slack 인터랙티브 OTP 입력 | Slack 알림에서 바로 OTP 코드를 입력할 수 있는 인터랙티브 메시지를 제공한다. 대시보드 접속 없이 Slack 내에서 수동 개입을 완료할 수 있다. | P2 | Phase 3 | 1. Slack 알림에 OTP 입력 필드가 포함된 인터랙티브 메시지가 전송된다 2. Slack에서 직접 OTP 코드를 입력하면 채증 플로우가 재개된다 3. 입력 완료 시 확인 메시지가 Slack에 표시된다 |

---

### 증거 비교 뷰 (UX-3)

| ID | 제목 | 설명 | 우선순위 | Phase | 수용 기준 |
|----|------|------|---------|-------|----------|
| FR-UI-044 | 증거 시간별 비교 뷰 | 동일 사이트의 시간별 채증 스크린샷을 좌우 비교(diff view)로 표시한다. 변경된 영역을 자동 하이라이트하여 "지속적 불법 운영"을 시각적으로 입증한다. | P2 | Phase 4 | 1. 동일 사이트의 2개 채증 결과를 좌우 비교할 수 있다 2. 1주/1개월/3개월 간격 비교 프리셋이 제공된다 3. 변경된 영역이 빨간색으로 하이라이트된다 4. "사이트 운영 지속성 입증" 타임라인 보고서가 자동 생성된다 |

---

## 비기능 요구사항 (UI 관련)

| ID | 제목 | 설명 | 목표값 |
|----|------|------|--------|
| NFR-UI-001 | 페이지 초기 로드 시간 | 서버 컴포넌트 기반 페이지 First Contentful Paint | 2초 이내 |
| NFR-UI-002 | 테이블 렌더링 성능 | 1,000행 데이터 테이블 렌더링 완료 | 500ms 이내 |
| NFR-UI-003 | 실시간 업데이트 지연 | SSE/WebSocket 이벤트 수신 후 UI 반영 | 3초 이내 |
| NFR-UI-004 | 차트 렌더링 성능 | 1년치 일별 데이터 차트 렌더링 | 1초 이내 |
| NFR-UI-005 | 접근성 준수 | WCAG 2.1 AA 기준 적합성 | 필수 기준 충족 |
| NFR-UI-006 | 브라우저 호환성 | Chrome, Edge, Firefox, Safari 최신 2개 버전 | 모든 기능 정상 동작 |
| NFR-UI-007 | 번들 크기 | 초기 JS 번들 크기 (gzip 기준) | 200KB 이내 |
| NFR-UI-008 | Lighthouse 성능 점수 | Performance, Accessibility, Best Practices | 각 90점 이상 |
